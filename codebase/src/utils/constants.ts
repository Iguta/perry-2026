import type { GoalType, Theme } from '../types/goal'
import type { Priority } from '../types/task'

export const PRIORITIES: Priority[] = ['Low', 'Medium', 'High']
export const GOAL_TYPES: GoalType[] = ['New Year', 'Monthly', 'Daily']
export const THEMES: Theme[] = ['Spiritual', 'Physical Fitness', 'Academics', 'Career']
